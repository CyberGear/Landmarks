apply plugin: 'java'

sourceCompatibility = "1.8"
targetCompatibility = "1.8"

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
//    compile 'org.codehaus.woodstox:woodstox-core-asl:4.4.1'
    compile 'com.squareup:javapoet:1.8.0'
    compile project(':landmarks')
}
